resources:
- ../base

patches:
- path: ./patches/db-persistentvolumeclaim.yaml

namespace: econia-mainnet

configMapGenerator:
- name: processsor-config
  files:
    - ./processor/config.yaml
  options:
    disableNameSuffixHash: true